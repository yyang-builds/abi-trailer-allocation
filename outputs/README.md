# Outputs

This directory is intended for generated artifacts such as:

- evaluation summaries
- exported figures
- intermediate experiment tables

Repository-tracked examples should be small and documentation-oriented. Large generated files belong in `outputs/generated/`, which is ignored by git.
